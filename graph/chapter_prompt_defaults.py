"""
写章与润色共用的默认提示片段。

思路：
1) 模型在接近目标字数时，常自动追加「主题回扣 + 排比/金句」式收尾，与「不剧透后文」混读后易被写成章末说教；
2) 用固定约束限制收束形态，不改变剧情事实，也不替代用户在 state 中配置的 style_constraint（二者叠加）。

边界：
- 若大纲要点本身要求强闭环（如「当场签约」），仍会发生情节收束；本约束针对的是收束之后的「额外升华段」。
"""

# 写章与润色共用的章末规则正文（中文名(EnglishName)：无额外结构化字段，仅作 prompt 片段）
DEFAULT_CHAPTER_ENDING_RULES = """【章末约束（默认）】
- 禁止在章节末尾单独用一段或多段做「主题升华、价值总结、排比收束、预言式旁白」；少用或不用「这一次他/她不会再……」「他知道，这只是开始」「XX，才是……的开始」类收束全章的句子。
- 若本章情节要点已写完，最后若干句优先落在具体场景：动作未停、对话停在半截、环境声或身体层面的感官细节（冷、疼、光、静、心跳）；可留白给下一章衔接。
- 「不总结后续剧情」指不剧透大纲外的后文，不等于必须在章末把本章意义讲透；避免用全书视角或上帝口吻总结本章。"""

# 润色阶段追加：要求对已有「套路尾段」做压缩或改写
DEFAULT_REFINE_CHAPTER_ENDING_EXTRA = """【润色与章末】
- 在遵守上文「章末约束（默认）」的前提下润色：若原文末段符合上述禁止项，请改为更克制的收束（场景/动作/感官），可删减说教句；不要因润色新增总结式尾段。
- 不改变已发生事实、人物关系与对话含义。"""
