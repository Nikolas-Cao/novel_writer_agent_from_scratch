---
description: Enforce clear comments for complex logic with examples
alwaysApply: true
---

# 复杂逻辑注释规范

- 保持良好的注释习惯：注释应解释“为什么这样做”和“实现思路”，而不是逐行复述代码表面行为。
- 当代码包含复杂分支、状态机、窗口滑动、重试回退、并发控制、边界条件处理等逻辑时，必须添加详细注释。
- 复杂逻辑优先在代码块上方写“块注释”，必要时在关键语句旁补充“行内注释”说明关键决策点。
- 注释建议包含以下信息：
  - 输入前提（数据形态、约束、可能为空的字段）
  - 核心步骤（按 1/2/3 描述算法或处理流程）
  - 关键取舍（为什么不用更直观方案、为何这样实现更稳妥）
  - 边界行为（空输入、超限、异常分支如何处理）
- 统一中英文字段格式（用于注释中的键名/字段说明）：
  - 推荐格式：`中文名(EnglishName)`，例如：`项目ID(project_id)`、`章节数(chapter_count)`。
  - 同一文件内保持一致：不要混用 `中文(English)`、`English(中文)`、`中文-English` 等多种写法。
  - 英文字段名统一使用 `snake_case`，避免同义字段多写法（如 `chapterCount` 与 `chapter_count` 混用）。
- 必要时提供简短实例，说明预期输入和预期输出，帮助读者快速验证理解。

## 注释示例模板

```python
# 思路：
# 1) 先按时间排序，保证窗口单调前进；
# 2) 再维护最近 N 条记录的滑动窗口；
# 3) 若命中阈值则提前返回，避免全量扫描。
#
# 输入示例：
# events = [{"t": 1, "score": 2}, {"t": 2, "score": 5}, {"t": 3, "score": 4}]
# threshold = 9, window_size = 2
#
# 预期结果：
# 在 t=3 时窗口分数为 5+4=9，返回 True。
```
